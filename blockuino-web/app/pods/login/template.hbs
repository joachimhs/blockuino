<div class="container">
  <div class="alert alert-info"><i class="glyphicon glyphicon-info-sign"></i> Blockuino bruker samme konto som på kodegenet.no. Dersom du ikke har en Kodegenet konto fra før, kan du opprette en
    på <a href="https://kodegenet.no/login" target="_blank">kodegenet.no/login</a></div>

  {{#if errorMessage}}
    <div class="alert alert-danger" role="alert">{{errorMessage}}</div>
  {{/if}}

  {{#if session.session.username}}
    <div>Du er logget inn med {{session.session.username}}. <button {{action "doLogOut"}}>Logg ut</button></div>
  {{else}}
    <form {{action "doLogIn" on="submit"}}>
      <div class="form-group">
        <label for="exampleInputEmail1">Email address</label>
        {{input type="email" value=model.email class="form-control" id="exampleInputEmail1" placeholder="Email"}}
      </div>
      <div class="form-group">
        <label for="exampleInputPassword1">Password</label>
        {{input type="password" value=model.password class="form-control" id="exampleInputPassword1"
                placeholder="Password"}}
      </div>
      <button type="submit" class="btn btn-default">Submit</button>
    </form>
  {{/if}}

</div>